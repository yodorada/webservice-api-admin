import React from 'react';
import TranslationUtils from '../../../utils/TranslationUtils';

const requiredField = (
  <TranslationUtils label="yodorada.errors.fields.required" />
);

export default requiredField;
